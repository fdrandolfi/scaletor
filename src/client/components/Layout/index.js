import React from "react";

import PRS from "../GuitarTemplates/PRS";

const Layout = () => (
  <section className="layout">
    <div className="layout__center">
      <PRS />
    </div>
  </section>
);

export default Layout;
